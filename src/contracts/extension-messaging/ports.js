/** @readonly */
export const PORT_PANEL = 'candidex-panel';

/** @readonly */
export const PORT_CONTENT = 'candidex-content';

/** Max length for free-text fields in runtime messages. */
export const MAX_TEXT_FIELD_LENGTH = 4000;

/** Max length for navigation paths. */
export const MAX_PATH_LENGTH = 512;